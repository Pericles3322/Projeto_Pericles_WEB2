{
  "name": "pc-stock",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "api": "json-server --watch db.json --port 3000",
    "json:server:routes": "json-server --watch db.json --routes routes.json --port 3000"
  },
  "dependencies": {
    "bootstrap": "^5.3.8",
    "uuid": "^14.0.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "json-server": "^0.17.4",
    "typescript": "^5.8.3",
    "vite": "^8.0.7"
  }
}
